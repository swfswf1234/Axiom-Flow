# Rudin 扫描教材解析评测

状态：Rejected
日期：2026-07-28

## 范围

实验使用 `rudin-scan-v1` 冻结的 12 个扫描教材页面。源产物标识为
`sha256:341544f3fa9ce6ac8bf3860b4d9f9e4e86b1d2778e2a8644c1cafb31267ed968`。
完整响应与页图只保存在被忽略的本地 `data/evaluation/rudin-scan-v1/`。

MinerU 3.4.4 隔离安装在 10 分钟内未产生包元数据，候选判定为环境不可用，没有生成质量分数。
百炼首次用 45 秒超时消耗 2 次调用且主、回退均超时；随后把超时提高到 180 秒，并把前 2 次
继续计入同一个 24 次上限。

## 结果

百炼累计使用 24 次调用，总耗时约 655 秒。9 页只返回单个局部块，2 页返回较长但截断且无
bbox 的 Markdown，仅第 295 页接近完整。评分卡平均分为 0.604，存在公式 0 分、来源证据 0 分和
多项页面大面积缺失，未达到 1.5 门槛。

`cost_estimate` 记录为 0 仅表示本次未取得可验证的计费回执，不能解释为外部调用没有成本。

## 结论

拒绝将当前 `qwen-vl-ocr` / `qwen-vl-plus` 页级提示路线用于 317 页整书解析。按照冻结计划，
不得扩大到 634 次调用或生成伪成功 `ParseRun`。下一轮必须先解决可安装的本地扫描数学解析器，
或更换能稳定返回完整页面结构的供应商方案，再复用同一 12 页回归。
