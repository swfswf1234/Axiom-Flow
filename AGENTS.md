# Axiom-Flow Agent 执行协议

## 项目目标

Axiom-Flow 是本地优先的技术 PDF 质量与知识审阅工作台。当前重点是解析质量和知识审阅，
不是已完成的学习助手。

## 修改前阅读顺序

1. 阅读 `docs/README.md`。
2. 阅读相关 `docs/architecture/`、`docs/design/` 与 `docs/adr/` 文档。
3. 在 `docs/architecture/code-map.md` 查找受影响代码的设计、测试与实现状态。
4. 阅读 `docs/trackers/current.md` 和关联计划。

## 事实来源优先级

运行代码和测试结果、已接受 ADR、架构文档、设计文档、计划、追踪器、历史资料依次递减。
历史资料仅解释背景，不能覆盖当前设计。

## 文档与代码追溯

- `docs/architecture/code-map.md` 是代码与文档对应关系的唯一事实源。
- 所有非平凡生产模块、评测脚本和测试文件必须在文件头写中文模块说明、
  `设计关联（DesignRef）` 和实现状态；`__init__.py`、纯常量和极短无业务语义文件可豁免。
- 活跃架构和设计文档必须声明 `关联代码`、`关联测试` 和 `关联 ADR`。
- 新增、移动或删除受管模块，以及改变其设计关联时，必须同步更新映射表、文件头和映射测试。
- `Legacy` 代码只能关联历史资料，不能被描述为当前目标架构的实现。

## 中文文档与注释

- 设计、计划、指南、模块 docstring、类 docstring 和公共函数 docstring 默认使用中文。
- 函数内部注释只解释意图、业务约束、异常原因、不可变条件、性能或安全风险；不逐行复述代码。
- 标识符、类型、API 字段和外部协议名称保持其既有英文形式；外部协议要求英文时可使用英文。

## 工作与关闭门禁

- 修改领域边界、公开 API、持久化语义、事实来源或解析路由前，先新增或更新 ADR。
- 每项实现必须关联计划，计划应包含范围、验证、回滚方式和完成条件。
- 外部模型实验前必须冻结假设、样本、预算和采纳门槛；实验结果必须经 ADR 接受后才成为设计决策。
- 任务只有在定向测试、适用回归/端到端测试和文档映射测试通过后才能关闭。可复现失败必须进入
  回归待办或明确标记为外部依赖问题。
