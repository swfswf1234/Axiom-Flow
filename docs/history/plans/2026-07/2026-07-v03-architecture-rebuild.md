# v0.3 架构重建

设计状态：Accepted
实现状态：Completed
最后更新：2026-07-27

## 范围

建立强类型领域边界、追加式运行历史、MySQL 持久任务、独立 Worker、`/api/v1`、任务化 Web
和自动化交付门禁。保留百炼供应商边界、MySQL、本地产物、Excel 显式发布和人工审阅原则。
学习聊天、向量检索、图数据库和正式 12 页解析评测不在本轮。

## 工作项

- [x] 审计 v0.2 并接受后台任务、版本化记录和重建保护 ADR。
- [x] 建立领域、应用、基础设施、API 和 Worker 分层。
- [x] 增加 v0.3 增量迁移和显式开发库重建工具。
- [x] 实现任务租约、幂等、进度、取消、恢复和任务级模型预算。
- [x] 用 `/api/v1` 与任务轮询重写 Web 主链。
- [x] 分离核心、开发和 Legacy 依赖，增加 GitHub CI。
- [x] 完成单元、MySQL 集成、双 Worker 和无网络端到端测试。

## 回滚与完成条件

代码提交按文档、领域/迁移、任务/API/Web、质量门禁分组。迁移前记录 `af_` 表清单；普通升级
不得删除旧数据。全量测试、迁移、文档映射、静态检查、Web 冒烟和差异审查通过后，标记完成并
一次性推送 `main`。

## 验证记录

2026-07-27 将运行库非破坏性升级至 `20260727_0002`。本地 Ruff 通过，全量测试 31 项通过；
v0.3 迁移、任务、API 和重建保护定向测试 13 项通过。API 与独立 Worker 双进程启动成功，
`/api/v1/health` 返回 `0.3.0`。Playwright 复用本机 Chrome 检查 1440x900 和 390x844 视口，
均无水平溢出；移动标签位于首屏。截图保存在被忽略的本地 `data/`，不进入仓库。
