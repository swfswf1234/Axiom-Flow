# Guides 人类手册收敛

设计状态：Accepted
实现状态：Completed
最后更新：2026-07-29

## 目标与范围

将 `docs/guides/` 收敛为开发与运维两本面向人类的当前手册。开发手册覆盖环境、迁移、测试和
提交门禁；运维手册区分当前可执行操作与平台无关的生产就绪缺口。本计划只修改文档与文档结构
测试，不改变生产代码、HTTP API、数据库或运行数据。

## 工作项

- [x] 将本地开发与测试指南合并为稳定的 `development.md`。
- [x] 重写 `operations.md`，补齐当前操作、受保护清理、恢复和故障处置。
- [x] 以 `OPS-001` 登记尚未实现的生产运维基线，不编造部署命令。
- [x] 归档旧测试指南并同步入口、规范、追踪器和历史索引。
- [x] 增加指南目录与关键边界门禁，完成专项和全量验证。

## 验证

文档结构、Markdown 链接和代码映射专项测试共 14 项通过；全量 Pytest 共 83 项通过；Ruff 与
`git diff --check` 通过。`prune_parse_runs` 和 `reset_dev_database` 的 CLI 帮助输出已与运维
手册逐项核对。

## 回滚与完成条件

回滚锚点为 `9f545dd`。活跃指南只保留索引、开发手册和运维手册；所有入口链接有效，测试规则
只有明确事实源，`OPS-001` 在运维手册和 backlog 双向登记，全部门禁通过后标记 Completed 并归档。
