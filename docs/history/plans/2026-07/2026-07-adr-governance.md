# ADR 治理与索引体系整理

设计状态：Accepted
实现状态：Completed
最后更新：2026-07-29

## 目标与范围

建立可扩展的 ADR 全局编号、元数据、索引、取代关系和自动门禁，并统一现有 12 份 ADR。
现有 ADR 路径和正文不移动、不重写；本计划不改变生产行为、公开 API 或持久化语义。

## 工作项

- [x] 新增 ADR 治理规范并更新模板与开发入口。
- [x] 补齐现有 ADR 的领域、决策阶段和双向取代元数据。
- [x] 将 ADR README 改为唯一登记表，声明下一个可用编号。
- [x] 增加 ADR 结构门禁并同步 code-map。
- [x] 通过文档专项和全量本地门禁。

## 验证

ADR、文档结构、Markdown 链接和代码映射专项测试共 17 项通过；Ruff、全量 Pytest 69 项、
JavaScript 语法和 `git diff --check` 通过。

## 回滚与完成条件

回滚锚点为 `d23fcea`。12 份 ADR 均只在原路径出现一次，元数据和取代关系一致，下一个编号为
0013，全部适用门禁已经通过，本计划移入历史计划目录。
