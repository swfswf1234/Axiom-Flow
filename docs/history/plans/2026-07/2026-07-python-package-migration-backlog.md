# Python 包结构迁移待办登记

设计状态：Accepted
实现状态：Completed
最后更新：2026-07-29

## 目标与范围

在待办中登记生产 Python 包从 `backend.*` 迁移到 `src/axiom_flow/*` 的后续结构任务。本计划
只修改追踪文档，不改变当前包结构、运行入口、导入路径或生产行为。

## 工作项

- [x] 增加 P1 包结构迁移待办及明确前置条件。
- [x] 通过文档结构、链接、映射和差异门禁。

## 验证

文档结构、Markdown 链接和代码映射专项测试共 12 项通过，`git diff --check` 通过。

## 回滚与完成条件

回滚锚点为 `54293d6`。待办只出现在 backlog，内容明确且文档门禁已经通过，本计划移入历史
计划目录。
