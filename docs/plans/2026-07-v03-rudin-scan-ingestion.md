# v0.3 Rudin 扫描教材解析

设计状态：Accepted
实现状态：Blocked
最后更新：2026-07-28

## 目标

对 `01_math_analysis` 中 317 页的《数学分析原理（原书第 3 版）》先完成扫描教材专项评测，
再使用达标解析器生成可恢复、可校验的整书解析运行。产物固定保存在内容寻址目录，包含文字、
页图、内容图、规范化 JSON、来源证据和清单。

本任务按 C 类实验、B 类确定性实现和 D 类正式数据操作依次执行。知识抽取、逐页人工接受、
工作簿和知识发布不在本轮。

## 工作项

- [x] 建立扫描教材单类别评测、不可变产物契约和失败语义。
- [x] 实现逐页持久化、断点续跑和产物 API。
- [x] 删除 v0.1 Legacy 运行代码、脚本、依赖和旧配置，保留历史文档。
- [x] 冻结 12 页 Rudin manifest，在最多 36 次外部调用内完成评测和评分。
- [x] 依据评分新增解析路由 ADR；当前两个候选均未达成可运行条件，整书停止。
- [x] 按 ADR 0010 删除 MinerU 与视觉回退，只使用 `qwen-vl-ocr` 完成 PDF 第 20–39 页试跑。
- [x] 核验 20 页页面、文字、图片、JSON、相对路径和 SHA-256 清单。
- [x] 严格抽检 PDF 第 20、24、29、34、39 页并形成报告；结果 3/5，质量门禁未通过。

## 验证

定向测试覆盖适配器归一化、空页、预算、断点续跑、产物清单、裁剪和 API；随后运行 MySQL
迁移测试、代码文档映射、Ruff 和全量 Pytest。正式运行必须有 317 个有序页面事实，每页有页图、
证据和质量结论，并有非空正文或显式空白页判定。

## 回滚

代码和迁移作为独立提交回滚；旧解析运行不覆盖。正式运行失败时保留 partial 清单、已完成页和
脱敏错误，不切换默认解析路由。原 PDF 与任何既有发布快照不删除。

## 完成条件

专项评测报告和路由 ADR 已形成；20 页运行成功且最终清单校验通过；五页人工抽检全部通过；
定向、全量和映射门禁全部通过；追踪器、设计、代码映射和工作记录同步完成。317 页整书解析
不属于本轮完成条件。

## 阻塞记录

2026-07-28 百炼候选使用全部 24 次预算，评分 0.604，未达到 1.5；MinerU 3.4.4 隔离安装在
10 分钟内未产生包元数据。ADR 0009 已拒绝当前路线。恢复必须满足 ADR 0009 的新候选门槛，
不得直接放宽预算或把局部响应解释为整书解析完成。

2026-07-28 ADR 0010 已接受修复后的 OCR 单模型限定试跑：冻结 PDF 第 20–39 页、每页最多 3 次、
总计 60 次调用。该决定只恢复工程链路与五页严格抽检，不放宽旧 12 页整书路线采纳门槛。

最终 `qwen-ocr-markdown-v2` 运行使用 22 次调用完成 20 页，manifest 哈希为
`b0f44ed0d9c6a5295773c5b429259e330ea6a3622596b5380361308305d1de35`。结构链路全部通过，但人工
抽检仅 3/5；PDF 第 20、24 页存在主要公式变量误识别。报告见
`../../evaluation/reports/rudin-qwen-ocr-20-v1.md`，因此质量任务保持阻塞且不启动整书。

## 已完成验证

2026-07-28 最终执行 Ruff、全量 Pytest、代码文档映射、Markdown 相对链接和
`git diff --check`；全量测试 44 项通过。运行库已显式升级到 `20260728_0003`，最终 ParseRun 与
产物保存在内容寻址目录；最终 v2 被显式选为当前运行，旧失败运行和 v1 运行已按 ADR 0011
清理为只保留摘要的 `pruned` 墓碑。
