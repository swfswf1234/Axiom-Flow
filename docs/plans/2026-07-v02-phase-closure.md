# v0.2 阶段性收尾

状态：Completed  
最后更新：2026-07-27

## 关闭范围

本记录关闭 v0.2 的单篇本地审阅与发布闭环，不关闭解析模型质量决策或学习交互。已交付能力为：

- PDF 导入、百炼视觉解析供应商边界与本地文字层证据。
- 页面、知识候选和关系的显式人工审阅。
- Excel 草稿导入导出、校验、显式发布和版本快照。
- MySQL `af_` 运行事实、Alembic 迁移和 Web 图谱浏览。

运行库迁移版本为 `20260727_0001`，已验证运行库共有 7 张 `af_` 表。历史 `app/` 保持
Legacy，不属于本关闭范围。

## 外部验证

百炼单页连通性预检使用主模型 `qwen-vl-ocr` 成功，报告为
`evaluation/reports/parser-v1-preflight.json`。预检过程中发现模型响应可能在 JSON 后附加文本，
已在供应商适配层修复首个 JSON 对象提取；初始脱敏诊断保留于
`evaluation/reports/parser-v1-preflight-initial.json`。预检仅证明调用链可用，不构成质量评分。

2026-07-27 使用项目 `xqfm_env` 执行验证：文档映射、预检与供应商适配定向测试共 10 项通过；
全量 `pytest -q` 共 23 项通过。`git diff --check` 亦通过。系统默认 Python 未安装项目依赖，
不作为本项目的验证解释器。

## 未关闭事项与恢复条件

`2026-07-parser-routing-experiment.md` 保持 `Blocked`。恢复需要用户提供可合法使用的数学 PDF，
覆盖数学文字、扫描数学、公式密集和表格/图示四类页面；随后冻结每类 3 页的匿名 manifest，
在 36 次调用上限内完成评分卡，并以 ADR 决定是否增加选择性路由。

学习对话、图片问答、练习、学习进度、批量处理、扫描件专项规则、向量检索和图数据库投影均为
后续阶段，不能通过当前单篇闭环的完成状态推定为已实现。
