# v0.3 解析结果工作台与存储收口

设计状态：Accepted
实现状态：Completed
最后更新：2026-07-28

## 目标与范围

把页面审阅收口为原始页图与当前 OCR 的单页对照工作台，并为同一文档建立显式当前 ParseRun、
共享页图和可审计清理语义。本轮保留最终 Rudin 20 页 v2 运行的完整产物，旧成功和失败运行只
保留摘要墓碑。知识抽取质量和 317 页整书解析不在本轮。

## 工作项

- [x] 增加 `20260728_0004` 迁移、当前运行选择历史和回填。
- [x] 实现显式提升、单页读取、运行摘要及 manifest v1/v2 校验。
- [x] 新运行改用文档级共享页图，硬链接优先、复制回退。
- [x] 实现默认 dry-run 的两阶段清理工具及引用保护。
- [x] Web 改为单页导航、原图/OCR 双栏、文本/Markdown/JSON 标签和折叠历史。
- [x] 完成定向、全量、映射、文档链接和差异门禁。
- [x] 停服、备份清单、升级运行库、显式选择 v2，再清理四个旧运行。

## 验证

定向测试覆盖迁移回填、选择历史、候选不自动提升、非法提升、共享页图、双版本 manifest、单页
API、摘要 API 以及清理的 dry-run/引用/路径/恢复/purge。随后执行全量 Pytest、Ruff、代码文档
映射、Markdown 链接和 `git diff --check`。运行数据操作后复验当前 manifest 和 PDF 第 20–39
页导航。

## 回滚

代码和迁移按独立差异回滚。清理阶段仅移动目录并保留 operation manifest；验证失败时恢复目录
和数据库备份。trash 未经显式 purge 不删除，原 PDF 与当前运行始终受保护。

## 完成条件

所有代码门禁通过；最终 v2 被显式设为当前；四个目标旧运行标记为 `pruned` 且只保留摘要；
trash 经复核后显式清空；Web 能逐页对照当前 20 页结果并查看历史摘要。

## 完成记录

运行库升级至 `20260728_0004`，最终 v2 保持当前且 manifest 哈希为
`b0f44ed0d9c6a5295773c5b429259e330ea6a3622596b5380361308305d1de35`。20 张页图已转为共享资产，
四个旧运行保留 `pruned` 墓碑并清空暂存目录。Chrome 验证桌面/移动布局、第 20/39 页导航和
历史抽屉均正常，控制台错误为 0。
