# v0.3 工程基线发布

设计状态：Accepted
实现状态：In Progress
最后更新：2026-07-28

## 目标

在不改变 HTTP、数据库和解析产物契约的前提下，彻底删除 `backend/app/`，使运行代码符合
领域、应用、基础设施、API、Worker 和装配根的依赖方向；随后以当前 20 页 OCR 审阅链路建立
`main` 与 `v0.3.0` 工程基线。公式 OCR 质量仍为 Blocked，317 页整书不在本期。

## 工作项

- [x] 接受 ADR 0012，并更新目标架构和代码映射。
- [x] 迁移配置、MySQL、产物、百炼、PDF pipeline 和工作簿模块。
- [x] 让应用任务仅依赖端口与执行工厂，API/Worker 经装配根取服务。
- [x] 删除根目录 Legacy 与 `backend/app/`，更新全部导入和启动命令。
- [x] 增加架构依赖、主流程、Web 和发布门禁。
- [ ] 分组提交并直接推送 `main`；CI 成功后创建 `v0.3.0` 标签。

## 验证

运行 Ruff、全量 Pytest、架构依赖、代码文档映射、Markdown 链接、JavaScript 语法、
`git diff --check`。启动 API 和 Worker，复验第 20/39 页、历史墓碑和产物下载，并用本机 Chrome
检查 1440×1000 与 390×844。

## 回滚与完成条件

代码按当前工作台基线、Backend 收口、发布文档三个提交组织；运行库已有逻辑备份。推送前检查
完整差异，推送后监控 CI。只有本地门禁、真实运行只读冒烟和远端 CI 全部通过，才创建标签并
把计划标记为 Completed。
